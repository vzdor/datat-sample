<h2>Products</h2>

<table id="products-tbl">
  <thead>
    <th>Title</th>
    <th>Price</th>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.title %></td>
        <td><%= product.price %></td>
      </tr>
    <% end %>
</table>
